@model BlogASP.Models.ArticleModel

<div class="container">
    <div class="text-center">
        <h2>@Model.Title</h2>
        <p>Category: @Model.Category</p>

        <div>
            <img src="@Model.Picture" width="361" class="img-fluid" alt="Article Image">
        </div>

        <div class="mt-4">
            <p>@Model.Description</p>
        </div>
    </div>

    <div class="mt-5 text-center">
        <h2>Comment Section</h2>

    </div>

    <!-- Related Articles -->
    <h2>Related Articles</h2>
    <div class="overflow-container" style="overflow-x: auto; white-space: nowrap;">
        <div class="row">
            @foreach (var article in ViewBag.ArticleRepository.GetRelatedArticles(Model.ArticleId))
            {
                <!-- Display related articles as needed -->
            }
        </div>
    </div>

    <!-- Most Starred Articles -->
    <h2>Top 3 Articles</h2>
    <div class="overflow-container" style="overflow-x: auto; white-space: nowrap;">
        <div class="row">
            @foreach (var article in ViewBag.ArticleRepository.GetMostStarredArticles())
            {
                <!-- Display most starred articles as needed -->
            }
        </div>
    </div>

</div>